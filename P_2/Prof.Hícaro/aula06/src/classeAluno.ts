class Aluno {
    nome: string;
    matricula: number;
    notas: number[];

    constructor(nome, matricula){
        this.nome = nome;
        this.matricula = matricula;
        this.notas = []
    }
    adicionarNota(notas: number[]){
        if( notas [] < 0 || notas [] > 10){

        }
    }
}